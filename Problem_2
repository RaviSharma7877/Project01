let str = "naman";
let count = [];
for (let i = str.length - 1; i >= 0; i--) {
    count = count + str[i];
}
if (str == count) {
    console.log(str + " is a Palindrome.");
} else {
    console.log(str + " is not a Palindrome.")
}
